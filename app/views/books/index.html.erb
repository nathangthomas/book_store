<h1>All Books</h1>
<% @books.each do |book| %>
  <section id="book-<%= book.id %>">
    <h2>Title: <%= book.title %></h2>
    <p>Number of Pages: <%= book.num_of_pages %></p>
    <p>Publication Year: <%= book.publication_year %></p>
    <p>Authors: <% book.authors.each do |author| %></p>
      <ul>
        <li><%= author.name %></li>
      </ul>
    <% end  %>
  </section>
  <% end  %>
